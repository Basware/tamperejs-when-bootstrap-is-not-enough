export interface SensorElements {
    wrapper: HTMLElement;
    expand: {
        sensor: HTMLElement;
        content: HTMLElement;
    },
    shrink: {
        sensor: HTMLElement;
        content: HTMLElement;
    }
}
